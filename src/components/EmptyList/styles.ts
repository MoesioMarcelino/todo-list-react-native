import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingTop: 48,
    paddingBottom: 48,
    paddingRight: 24,
    paddingLeft: 48,

    justifyContent: "center",
    alignItems: "center",
    gap: 16,
  },

  title: {
    color: "#808080",
    fontSize: 14,
    fontWeight: "bold",
  },

  description: {
    color: "#808080",
    fontSize: 14,
  },
});
